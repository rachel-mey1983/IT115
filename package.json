{
  "name": "it115",
  "version": "1.0.0",
  "description": "$ git checkout -b feature-1 Switched to a new branch 'feature-1'",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
"scripts":]
{ "test": "jest"
}
javascript
// index.test.js
test('Sample test', () => {
  expect(1 + 1).toBe(2);
});